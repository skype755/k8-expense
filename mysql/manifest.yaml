apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql
  namespace: k8-expense
  # lables of deployment resource
  labels:
    app: mysql
    tier: mysql
    project: k8-expense
spec:
    replicas: 1
    selector:
      matchLabels:
        app: mysql
        tier: mysql
        project: k8-expense
    template:
      metadata:
        labels:
          app: mysql
          tier: mysql 
          project: k8-expense
      spec:
        containers:
        - name: nginx
          image: saimaddala/mysql:1.0.0